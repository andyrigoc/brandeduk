(function desktopExperienceGate() {
  'use strict';

  // The desktop site is live. Keep this loader as a no-op because older
  // pages still reference it while the site is migrated incrementally.
}());
